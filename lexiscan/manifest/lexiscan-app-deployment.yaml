apiVersion: apps/v1
kind: Deployment
metadata:
  name: lexiscan-app-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: lexiscan-app
  template:
    metadata:
      labels:
        app: lexiscan-app
    spec:
      containers:
        - name: lexiscan-app
          image: dashbah/lexiscan-app:latest
          env:
            - name: SPRING_DATASOURCE_URL
              valueFrom:
                secretKeyRef:
                  name: db-secret
                  key: datasource-url
          volumeMounts:
            - mountPath: /app/build/libs
              name: app-volume
      volumes:
        - name: app-volume
          emptyDir: {}
        - name: config-volume
          configMap:
            name: lexiscan-app-configmap
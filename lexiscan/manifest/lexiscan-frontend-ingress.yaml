apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: lexiscan-frontend-ingress
  namespace: default
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: lexiscan-api.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: lexiscan-frontend-service
                port:
                  number: 3005